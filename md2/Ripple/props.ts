import { propsDesign, subClassesDesign } from './props.design'
import { props as propsConstructor } from '../../constructors/Ripple/props'

export const subClasses = {
  ...subClassesDesign

  // Subclass list

}

export const props = {
  ...propsDesign,
  ...propsConstructor

  // Values

  // Status

  // Options
}
