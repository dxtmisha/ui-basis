import { ButtonDesignPropsValueType } from './types'

export class ButtonIcon {
  // eslint-disable-next-line no-useless-constructor
  constructor (
    protected readonly props: ButtonDesignPropsValueType
  ) {
  }
}
